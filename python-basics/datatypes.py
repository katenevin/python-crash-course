cover purpose, literal syntax, methods

list, list.append, list.pop, list.__add__, list.__contains__, list.extend, list.sort, list.reverse, homogeneous

dict, dict.get, dict.keys, dict.iterkeys/viewkeys, dict.values, dict.itervalues/viewvalues, dict.items, dict.iteritems/viewitems, dict.setdefault, dict.update, dict.pop

tuple, immutable, heterogeneous

set, set.add, set.difference, set.pop, set.union, set.intersection, set.__or__, set.__and__

str, str.split, str.format, str.__mod__, str.join, str.lower, str.upper, str.strip

int, long, float
	assume from __future__ import division

bool, True/False 

None

