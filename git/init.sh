echo -e "The init command creates a new, empty git repository. Run this in any directory to begin tracking files.
A useful option here is --bare. This allows you to create a repository suitable for accepting a push (more on this later).\n"

echo "$ mkdir clone1
$ cd clone1
$ git init"

mkdir clone1
cd clone1
git init

