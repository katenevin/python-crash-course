echo -e "The clone command creates a copy of a repository. Use this any time you're getting a local copy from a remote source for the purpose of editing.
Changes to a cloned repository will be merged upstream with the push command and downstream with fetch and merge or pull commands.\n"

echo "$ cd ..
$ git clone repo clone2"

git clone repo clone2
